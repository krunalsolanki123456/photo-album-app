# Flipora Landing Page Design Lock

Approved reference: `APPROVED_LANDING_REFERENCE.png`

The landing page in `src/main.jsx` uses `lp-*` classes. The matching CSS is the final
`FLIPORA LANDING PAGE — LOCKED REFERENCE DESIGN` block in `src/styles.css`.

Do not redesign or reinterpret the landing page without explicit approval. In particular:
- Keep the approved hero/header/share-bar/why-choose/premium/pricing/review/footer structure.
- `Create for Every Occasion` must show 4 equal cards on desktop.
- Slider arrows move exactly one card at a time; after the last valid group it loops to the first.
- All occasion cards must keep identical dimensions.
- Tablet shows 2 cards, mobile shows 1 card.
